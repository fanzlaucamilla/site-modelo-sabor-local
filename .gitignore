# Dependências
/node_modules

# Build do Next.js
/.next
/out

# Variáveis de ambiente
.env*.local

# Sistema / editores
.DS_Store
*.log
next-env.d.ts

# Configuração local do Claude Code / editor (não vai para os alunos)
.claude/

# Material do professor (roteiros de aula) — não publicar para os alunos
AULA*.md
